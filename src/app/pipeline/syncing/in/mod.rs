//! Houses pipeline related to incoming user syncs.
//! That is, partners returning user sync calls to
//! us with their buyeruid values in which case we
//! are expected to host the match table. That is
//! typically for demand partners but could be
//! for supply also.